const gameWidth = 1080 / 2;
const gameHeight = 1920 / 2;

export default {
  debug: false,
  gameWidth: gameWidth,
  gameHeight: gameHeight,
  randomSeed: 1,
  tileEdgeLength: 32,
  row: 24,
  column: 16,
};
