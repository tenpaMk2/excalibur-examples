const gameWidth = 32 * 9 * 2;
const gameHeight = 32 * 16 * 2;

export default {
  debug: false,
  gameWidth: gameWidth,
  gameHeight: gameHeight,
  gravity: 980,
  moveSpeed: 100,
  jumpSpeed: 500,
};
